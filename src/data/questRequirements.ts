import type { QuestRequirement } from '../types';

// Fallback quest requirements for testing
export const questRequirements: Record<string, QuestRequirement> = {
    "Cook's Assistant": {
        name: "Cook's Assistant",
        skills: [],
        questPrerequisites: []
    }
};